---
marp: true
theme: default
paginate: true
size: 16:9
---

<!-- _class: lead -->

# 디지털 도어락 — 입력부

## 버튼 입력 정제 · 매핑

`input_manager.v` / `button_debounce.v`

FPGA Cyclone II · 1 kHz · Quartus II 13.0

---

# 입력부의 역할

> 물리 버튼의 **지저분한 전기 신호**를
> **깨끗한 디지털 제어 신호**로 변환

- 입력: 13개 택트 스위치 `tact_sw[12:0]`
- 출력: `digit_in[3:0]`, `key_valid`, `enter`, `change`, `auto_open`
- 동작: **디바운싱 → 매핑 → 게이팅**

---

# 신호 흐름

```text
tact_sw[12:0]
   │
   ▼  button_debounce × 13   (2FF 동기화 + 20 ms 디바운스)
btn_state[12:0]
   │
   ├─ |btn_state[9:0]       → key_valid
   ├─ 우선순위 인코더         → digit_in
   ├─ btn_state[10]         → enter
   ├─ btn_state[11]         → change
   └─ btn_state[12]         → auto_open
```

---

# 키 매핑

| 스위치 | 출력 신호 |
|---|---|
| `tact_sw[0]~[9]` | `key_valid=1`, `digit_in = 0~9` |
| `tact_sw[10]` | `enter=1` |
| `tact_sw[11]` | `change=1` |
| `tact_sw[12]` | `auto_open=1` |

---

# 핵심 기술 ① 버튼 디바운싱

**문제** : 버튼은 누를 때 채터링(수 ms간 0↔1 떨림) 발생

**해결** (`button_debounce.v`)
- **2단 FF 동기화** → 메타스테이빌리티 방지
- **카운터** 로 값이 20 ms 연속 안정해야 `btn_state` 갱신
- 파라미터로 계산 : `MAX_COUNT = (CLK_FREQ/1000) × DEBOUNCE_MS`

```verilog
sync_0 <= btn_in;  sync_1 <= sync_0;                 // 2FF 동기화
if (sync_1 == btn_state)        counter <= 0;        // 흔들리면 리셋
else if (counter == MAX_COUNT)  btn_state <= sync_1; // 안정 시 확정
```

---

# 핵심 기술 ② generate 병렬 인스턴스화

13개 버튼 → 디바운서 13개를 **for문으로 자동 복제**

```verilog
genvar i;
generate
    for (i = 0; i < 13; i = i + 1) begin : debouncers
        button_debounce #(.CLK_FREQ_HZ(CLK_FREQ_HZ),
                          .DEBOUNCE_MS(DEBOUNCE_MS)) u_db (
            .clk(clk), .reset_n(reset_n),
            .btn_in(tact_sw[i]), .btn_state(btn_state[i]),
            .btn_pulse()              // 미사용 출력 빈 포트
        );
    end
endgenerate
```

- 코드 1벌 → 합성 시 **하드웨어 13개로 펼쳐짐**
- 파라미터 전달로 모든 채널 동일 사양 보장

---

# 핵심 기술 ③ 숫자 감지 + 우선순위 인코더

```verilog
assign key_valid = (|btn_state[9:0]) && !lock;     // OR 리덕션
assign digit_in  =
    btn_state[0] ? 4'd0 :
    btn_state[1] ? 4'd1 :
    ...
    btn_state[9] ? 4'd9 : 4'd0;                     // 우선순위 인코더
```

- **OR 리덕션 `|`** : 0~9 중 하나라도 눌리면 `key_valid=1`
- **삼항 체인** : 동시 입력 시 **낮은 번호 우선** (10채널 → 4bit 인코딩)

---

# 핵심 기술 ④ lock 입력 게이팅

```verilog
assign key_valid = (|btn_state[9:0]) && !lock;
assign enter     = btn_state[10] && !lock;
assign change    = btn_state[11] && !lock;
assign auto_open = btn_state[12] && !lock;
```

- `lock=1` 이면 모든 출력 신호를 **소스 레벨에서 강제 0**
- 입력 자체를 차단하는 **AND 게이팅** 구조

---

# 설계 포인트 정리

| 기술 | 효과 |
|---|---|
| 2FF 동기화 + 카운터 디바운스 | 채터링 · 메타스테이빌리티 제거 |
| `generate` 병렬 복제 | 13채널을 코드 1벌로 |
| 파라미터화(`#(...)`, `$clog2`) | 클럭/보드 무관 재사용 |
| OR 리덕션 + 우선순위 인코더 | 숫자 감지 · 변환 |
| lock AND 게이팅 | 입력 차단 |

> **순차논리(정제)** 는 하위 모듈 · **조합논리(매핑·게이팅)** 는 상위 모듈

---

<!-- _class: lead -->

# 감사합니다

질문 환영합니다
